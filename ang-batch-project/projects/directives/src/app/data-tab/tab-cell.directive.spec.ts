import { TabCellDirective } from './tab-cell.directive';

describe('TabCellDirective', () => {
  it('should create an instance', () => {
    const directive = new TabCellDirective();
    expect(directive).toBeTruthy();
  });
});
