import { DataFliterPipe } from './data-fliter.pipe';

describe('DataFliterPipe', () => {
  it('create an instance', () => {
    const pipe = new DataFliterPipe();
    expect(pipe).toBeTruthy();
  });
});
